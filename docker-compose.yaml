  services: 
  postgres:
      image: postgres:17
      container_name: postgres

      environment:
        POSTGRES_USER: admin
        POSTGRES_PASSWORD: qwerty
        POSTGRES_DB: pots_db

      ports:
        - "5432:5432"

      volumes:
        - postgres_data:/var/lib/postgresql/data
  pgadmin:
      image: dpage/pgadmin4
      container_name: pots-pgadmin

      environment:
        PGADMIN_DEFAULT_EMAIL: admin@pots.com
        PGADMIN_DEFAULT_PASSWORD: pots123

      ports:
        - "5050:80"

      depends_on:
        - postgres   

  volumes:
  postgres_data:      